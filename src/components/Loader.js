import React, { useRef } from "react";

const Loader = () => {
  return (
      <div className="text-center mt-9">
        <i className="fa fa-cog fa-spin" style={{ fontSize: "50px" }} />
      </div>
  );
};

export default Loader;
